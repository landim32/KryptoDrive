<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="KryptoDrive.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:KryptoDrive.Pages"
    Shell.FlyoutBehavior="Disabled"
    Title="KryptoDrive">

    <ShellContent
        Title="Login"
        ContentTemplate="{DataTemplate pages:LoginPage}"
        Route="login" />

    <ShellContent
        Title="Explorador"
        ContentTemplate="{DataTemplate pages:FileExplorerPage}"
        Route="explorer" />

</Shell>
